package com.code.devtask.integration.common.requests;

public class BaseApi {
    // TODO: apiUrlは環境ごとに異なるため、記述する場所は要検討(Config等)

    protected static final String baseUrl = "http://localhost:8080";
}
